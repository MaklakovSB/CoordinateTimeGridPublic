<UserControl x:Class="WPF.CTG.CoordinateTimeGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:WPF.CTG.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF.CTG"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="CoordinateTimeGr"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:DoubleInvertSignConverter x:Key="Disc" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Row="0"
                       Grid.Column="0"
                       Fill="Black"
                       Stroke="Transparent" />

            <Canvas x:Name="_coordinateViewPort"
                    Grid.Row="1"
                    Grid.Column="1"
                    Background="Transparent"
                    ClipToBounds="True"
                    >

                <!--<local:CoordinatePlane x:Name="_scalableCoordinatePlane"
                                       Canvas.Left="{Binding CanvasLeft}"
                                       Canvas.Top="{Binding CanvasTop}">


                </local:CoordinatePlane>-->
                <local:ScalableCoordinatePlane x:Name="_scalableCoordinatePlane"
                                               Canvas.Left="{Binding CanvasLeft}"
                                               Canvas.Top="{Binding CanvasTop}"
                                               Width="5000"
                                               Height="1000"
                                               GridColor="{Binding Path=GridColor, ElementName=CoordinateTimeGr}"
                                               />
            </Canvas>
        </Grid>

        <ScrollBar Grid.Row="0"
                   Grid.Column="1"
                   Maximum="{Binding ActualHeight,
                                     ElementName=_coordinatePlane,
                                     Converter={StaticResource Disc}}"
                   Orientation="Vertical"
                   Visibility="{Binding VerticalScrollBarVisibility}"
                   Value="{Binding Path=TopY,
                                   Mode=TwoWay,
                                   Converter={StaticResource Disc},
                                   ElementName=_coordinatePlane}" />

        <ScrollBar Grid.Row="1"
                   Grid.Column="0"
                   Maximum="{Binding MaximumScrlX}"
                   Orientation="Horizontal"
                   Visibility="{Binding HorizontalScrollBarVisibility}"
                   Value="{Binding Path=LeftX,
                                   Mode=TwoWay,
                                   Converter={StaticResource Disc},
                                   ElementName=_coordinatePlane}" />

    </Grid>
</UserControl>

<UserControl x:Class="WPF.CTG.CoordinateTimeGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:WPF.CTG.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF.CTG"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:DoubleInvertSignConverter x:Key="Disc" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Row="0"
                       Grid.Column="0"
                       Fill="Black"
                       Stroke="Transparent" />

            <!--<Canvas Grid.Row="0"
                    Grid.Column="1"
                    x:Name="_canvas001"
                    Background="#FF000000"
                    ClipToBounds="True"
                    Height="20">
                <Canvas x:Name="_canvas002"
                        Canvas.Left="{Binding CanvasLeft}"
                        Width="{Binding ActualWidth, ElementName=_canvasChild}"
                        Height="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Canvas.Background>
                        <DrawingBrush TileMode="FlipXY"
                                      Viewport="0,0,10,10"
                                      ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing Brush="DarkGray">
                                    <GeometryDrawing.Geometry>

                                        <PathGeometry>
                                            <PathFigure IsClosed="True" StartPoint="10,100">
                                                <LineSegment Point="100,100" />
                                                <LineSegment Point="100,50" />
                                            </PathFigure>
                                        </PathGeometry>

                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Canvas.Background>
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform X="{Binding MoveX}" />
                            <ScaleTransform CenterX="{Binding CenterX}"
                                            ScaleX="{Binding ScaleRateX}"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>
                </Canvas>
            </Canvas>-->

            <Canvas x:Name="_canvasParent"
                    Grid.Row="1"
                    Grid.Column="1"
                    Background="#FF3A3A3A"
                    ClipToBounds="True"
                    SizeChanged="_canvasParent_SizeChanged">
                <local:CoordinatePlane x:Name="_coordinatePlane"
                                       Canvas.Left="{Binding CanvasLeft}"
                                       Canvas.Top="{Binding CanvasTop}" />
            </Canvas>

            <!--<Canvas Grid.Row="1" Grid.Column="0"
                    x:Name="_canvas003"
                    Background="#FF000000"
                    ClipToBounds="True"
                    Width="20">
                <Canvas x:Name="_canvas004"
                        Canvas.Top="{Binding CanvasTop}"
                        Width="20"
                        Height="{Binding ActualHeight, ElementName=_canvasChild}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Canvas.Background>
                        <DrawingBrush TileMode="FlipXY"
                                      Viewport="0,0,10,10"
                                      ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing Brush="DarkGray">
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup FillRule="EvenOdd">
                                            <RectangleGeometry Rect="0,0,10,10" />
                                            <RectangleGeometry Rect="1,1,9,9" />
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Canvas.Background>
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform Y="{Binding MoveY}" />
                            <ScaleTransform CenterY="{Binding CenterY}"
                                            ScaleY="{Binding ScaleRateY}"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>
                </Canvas>
            </Canvas>-->

            <!--
                Maximum="{Binding ActualWidth,
                ElementName=_coordinatePlane}"
            -->

        </Grid>

        <ScrollBar Grid.Row="0"
                   Grid.Column="1"
                   Maximum="{Binding ActualHeight,
                                     ElementName=_coordinatePlane,
                                     Converter={StaticResource Disc}}"
                   Orientation="Vertical"
                   Visibility="{Binding VerticalScrollBarVisibility}"
                   Value="{Binding Path=TopY,
                                   Mode=TwoWay,
                                   Converter={StaticResource Disc},
                                   ElementName=_coordinatePlane}" />

        <ScrollBar Grid.Row="1"
                   Grid.Column="0"
                   Maximum="{Binding MaximumScrlX}"
                   Orientation="Horizontal"
                   Visibility="{Binding HorizontalScrollBarVisibility}"
                   Value="{Binding Path=LeftX,
                                   Mode=TwoWay,
                                   Converter={StaticResource Disc},
                                   ElementName=_coordinatePlane}" />

    </Grid>
</UserControl>
